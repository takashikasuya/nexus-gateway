services:
  gateway:
    environment:
      TELEMETRY_SINK: dtdpf
      DTDPF_POINT_CONFIG_FILE: /fixtures/pointConfig.dtdpf.json
      DTDPF_EVENTHUB_CONNECTION_STRING: ${DTDPF_EVENTHUB_CONNECTION_STRING:?set DTDPF_EVENTHUB_CONNECTION_STRING}
      DTDPF_EVENTHUB_NAME: ${DTDPF_EVENTHUB_NAME:-telemetry}
      DTDPF_EVENTHUB_TRANSPORT: ${DTDPF_EVENTHUB_TRANSPORT:-amqp-tcp}