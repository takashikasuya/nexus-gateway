// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.6.2
// - protoc             (unknown)
// source: gateway_ingress.proto

package gatewaybridge

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	GatewayIngress_StreamTelemetry_FullMethodName = "/gatewaybridge.GatewayIngress/StreamTelemetry"
)

// GatewayIngressClient is the client API for GatewayIngress service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// Ingress: client-streaming telemetry. Separate stream + scale unit from Egress (§3-1).
//
// The canonical contract is point-id based (#181): BuildingOS and the gateway SHARE the point
// list, so the gateway resolves its protocol-native addressing (BACnet device/object/instance,
// vendor unit ids, …) to a `point_id` locally. A frame therefore carries only what BuildingOS
// cannot derive: routing/provenance (`gateway_id`), identity (`point_id`), and the reading
// (`value` + `timestamp`). Static per-point metadata (building/device/name/…) is NOT sent every
// frame — BuildingOS enriches it from the digital twin by `point_id`.
//
// The Ingress pod holds no state. Each frame is enriched and published straight to
// building-os.validated.telemetry via a JetStream publish-ack (#187), so `accepted` means the frame
// was durably persisted to the stream (not merely sent). A frame with an unknown `point_id`, whose
// `gateway_id` does not own that point, or whose publish was not acked, is skipped (logged +
// metered); the stream continues and StreamAck.accepted counts only successfully-persisted frames.
type GatewayIngressClient interface {
	StreamTelemetry(ctx context.Context, opts ...grpc.CallOption) (grpc.ClientStreamingClient[TelemetryFrame, StreamAck], error)
}

type gatewayIngressClient struct {
	cc grpc.ClientConnInterface
}

func NewGatewayIngressClient(cc grpc.ClientConnInterface) GatewayIngressClient {
	return &gatewayIngressClient{cc}
}

func (c *gatewayIngressClient) StreamTelemetry(ctx context.Context, opts ...grpc.CallOption) (grpc.ClientStreamingClient[TelemetryFrame, StreamAck], error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	stream, err := c.cc.NewStream(ctx, &GatewayIngress_ServiceDesc.Streams[0], GatewayIngress_StreamTelemetry_FullMethodName, cOpts...)
	if err != nil {
		return nil, err
	}
	x := &grpc.GenericClientStream[TelemetryFrame, StreamAck]{ClientStream: stream}
	return x, nil
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type GatewayIngress_StreamTelemetryClient = grpc.ClientStreamingClient[TelemetryFrame, StreamAck]

// GatewayIngressServer is the server API for GatewayIngress service.
// All implementations should embed UnimplementedGatewayIngressServer
// for forward compatibility.
//
// Ingress: client-streaming telemetry. Separate stream + scale unit from Egress (§3-1).
//
// The canonical contract is point-id based (#181): BuildingOS and the gateway SHARE the point
// list, so the gateway resolves its protocol-native addressing (BACnet device/object/instance,
// vendor unit ids, …) to a `point_id` locally. A frame therefore carries only what BuildingOS
// cannot derive: routing/provenance (`gateway_id`), identity (`point_id`), and the reading
// (`value` + `timestamp`). Static per-point metadata (building/device/name/…) is NOT sent every
// frame — BuildingOS enriches it from the digital twin by `point_id`.
//
// The Ingress pod holds no state. Each frame is enriched and published straight to
// building-os.validated.telemetry via a JetStream publish-ack (#187), so `accepted` means the frame
// was durably persisted to the stream (not merely sent). A frame with an unknown `point_id`, whose
// `gateway_id` does not own that point, or whose publish was not acked, is skipped (logged +
// metered); the stream continues and StreamAck.accepted counts only successfully-persisted frames.
type GatewayIngressServer interface {
	StreamTelemetry(grpc.ClientStreamingServer[TelemetryFrame, StreamAck]) error
}

// UnimplementedGatewayIngressServer should be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedGatewayIngressServer struct{}

func (UnimplementedGatewayIngressServer) StreamTelemetry(grpc.ClientStreamingServer[TelemetryFrame, StreamAck]) error {
	return status.Error(codes.Unimplemented, "method StreamTelemetry not implemented")
}
func (UnimplementedGatewayIngressServer) testEmbeddedByValue() {}

// UnsafeGatewayIngressServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to GatewayIngressServer will
// result in compilation errors.
type UnsafeGatewayIngressServer interface {
	mustEmbedUnimplementedGatewayIngressServer()
}

func RegisterGatewayIngressServer(s grpc.ServiceRegistrar, srv GatewayIngressServer) {
	// If the following call panics, it indicates UnimplementedGatewayIngressServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&GatewayIngress_ServiceDesc, srv)
}

func _GatewayIngress_StreamTelemetry_Handler(srv interface{}, stream grpc.ServerStream) error {
	return srv.(GatewayIngressServer).StreamTelemetry(&grpc.GenericServerStream[TelemetryFrame, StreamAck]{ServerStream: stream})
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type GatewayIngress_StreamTelemetryServer = grpc.ClientStreamingServer[TelemetryFrame, StreamAck]

// GatewayIngress_ServiceDesc is the grpc.ServiceDesc for GatewayIngress service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var GatewayIngress_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "gatewaybridge.GatewayIngress",
	HandlerType: (*GatewayIngressServer)(nil),
	Methods:     []grpc.MethodDesc{},
	Streams: []grpc.StreamDesc{
		{
			StreamName:    "StreamTelemetry",
			Handler:       _GatewayIngress_StreamTelemetry_Handler,
			ClientStreams: true,
		},
	},
	Metadata: "gateway_ingress.proto",
}
